#ifndef GLOBALS_H
#define GLOBALS_H

extern int option_force;        // -f
extern int option_interactive;  // -i
extern int option_no_clobber;   // -n
extern int option_verbose;      // -v
extern int option_help;      // -h

#endif // GLOBALS_H
